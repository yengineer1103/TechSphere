<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FileSharingApplication">
        <activity
            android:name="com.example.fileshare.adapter.PdfViewActivity"
            android:exported="true" />
        <activity
            android:name="com.example.fileshare.adapter.ShowAllReceivedFiles"
            android:exported="true" />
        <activity
            android:name="com.example.fileshare.adapter.ShareFile"
            android:exported="true" />
        <activity
            android:name="com.example.fileshare.adapter.UserResetPasswordActivity"
            android:exported="true" />
        <activity
            android:name="com.example.fileshare.adapter.UserLogin"
            android:exported="true" />
        <activity
            android:name="com.example.fileshare.adapter.UserRegister"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>